# API reference

The public API is available at the top level of the `pertema` package (for example
`import pertema; pertema.default_model()`). The classes and functions below are generated from the source
docstrings.

## Scoring

::: pertema.scoring.PertEMAModel

::: pertema.scoring.default_model

## Featurization

::: pertema.featurize.Featurizer

::: pertema.featurize.default_featurizer

## Ingestion

::: pertema.ingest.parse_long_csv

## Report

::: pertema.report.render_report
